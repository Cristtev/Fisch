--========================================================--
-- RUISHI HUB || FISCH EDITION (COMPLETE REWRITE)
--========================================================--

local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local TeleportService = game:GetService("TeleportService")

local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

-- Cleanup Existing Instances
if playerGui:FindFirstChild("RuishiFischGui") then
	playerGui.RuishiFischGui:Destroy()
end
if playerGui:FindFirstChild("CyberpunkIntroGUI") then
	playerGui.CyberpunkIntroGUI:Destroy()
end

-- ScreenGui Container
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "RuishiFischGui"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = playerGui

--========================================================--
-- LOCATIONS DATA
--========================================================--
local Islands = {
	["Ancient Isle"]        = Vector3.new(6027, 198, 315),
	["Atlantis"]            = Vector3.new(-4454, -605, 1829),
	["Birch Cay"]           = Vector3.new(1680, 135, -2410),
	["Castaway Cliff"]      = Vector3.new(392, 200, -1772),
	["Cryogenic Canal"]     = Vector3.new(20280, 705, 5730),
	["Desolate Deep"]       = Vector3.new(-1630, -215, -2815),
	["Ethereal Abyss"]      = Vector3.new(-3991, -564, 1808),
	["Forsaken Shores"]     = Vector3.new(-2485, 133, 1537),
	["Frenzy of War"]       = Vector3.new(-8792, -2761, 826),
	["Glacial Grotto"]      = Vector3.new(19870, 1155, 5490),
	["Grand Riff"]          = Vector3.new(-3558, 151, 485),
	["Keepers Sanctuary"]   = Vector3.new(-3150, 850, -2200),
	["Lost Jungle"]         = Vector3.new(-2658, 148, -2039),
	["Moosewood"]           = Vector3.new(472, 150, 239),
	["Northern Expedition"] = Vector3.new(19645, 130, 5225),
	["Poseidon Temple"]     = Vector3.new(-4037, -558, 912),
	["Roslit Bay"]          = Vector3.new(-1468, 132, 691),
	["Scoria Reach"]        = Vector3.new(-5186, 143, -1488),
	["Snowcap Island"]      = Vector3.new(2645, 155, 2375),
	["Sunken Depths"]       = Vector3.new(-4936, -595, 1819),
	["Sunken Trial"]        = Vector3.new(-4782, -595, 1827),
	["Sunstone Island"]     = Vector3.new(-973, 222, -1002),
	["Terrapin Island"]     = Vector3.new(-225, 141, 1930),
	["Tidefall"]            = Vector3.new(3182, -1099, 739),
	["Volcanic Vents"]      = Vector3.new(-3489, -2258, 3809),
	["Zeus Sanctuary"]      = Vector3.new(-4296, -627, 2663)
}

local NPCs = {
	["Ancient Merchant"]    = Vector3.new(6050, 195, 280),
	["Angler NPC"]          = Vector3.new(480, 152, 300),
	["Appraiser (Moosewood)"] = Vector3.new(450, 150, 250),
	["Bait Merchant"]       = Vector3.new(-1480, 135, 710),
	["Castaway Innkeeper"]  = Vector3.new(410, 202, -1780),
	["Desolate Merchant"]   = Vector3.new(-1650, -210, -2820),
	["Forsaken Innkeeper"]  = Vector3.new(-2460, 135, 1560),
	["Islander Merchant"]   = Vector3.new(-950, 225, -970),
	["Lighthouse Keeper"]   = Vector3.new(510, 160, 180),
	["Moosewood Innkeeper"] = Vector3.new(490, 150, 220),
	["Roslit Innkeeper"]    = Vector3.new(-1440, 132, 680),
	["Shipwright"]          = Vector3.new(530, 148, 260),
	["Snowcap Merchant"]    = Vector3.new(2620, 158, 2380),
	["Sunstone Merchant"]   = Vector3.new(-990, 220, -960),
	["Terrapin Innkeeper"]  = Vector3.new(-200, 142, 1960)
}

local BaitItems = {
	"Bait Crate",
	"Quality Bait Crate",
	"Coral Geode",
	"Volcanic Geode"
}

--========================================================--
-- CYBERPUNK INTRO LOADING SCREEN (MATCHED TO REFERENCE)
--========================================================--
local IntroGui = Instance.new("ScreenGui")
IntroGui.Name = "CyberpunkIntroGUI"
IntroGui.ResetOnSpawn = false
IntroGui.Parent = playerGui

local IntroFrame = Instance.new("Frame")
IntroFrame.Size = UDim2.new(0, 360, 0, 240)
IntroFrame.Position = UDim2.new(0.5, -180, 0.5, -120)
IntroFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 25)
IntroFrame.BorderSizePixel = 0
IntroFrame.Parent = IntroGui

local IntroCorner = Instance.new("UICorner")
IntroCorner.CornerRadius = UDim.new(0, 14)
IntroCorner.Parent = IntroFrame

local IntroStroke = Instance.new("UIStroke")
IntroStroke.Color = Color3.fromRGB(0, 255, 204)
IntroStroke.Thickness = 2
IntroStroke.Parent = IntroFrame

local IntroLogo = Instance.new("ImageLabel")
IntroLogo.Size = UDim2.new(0, 50, 0, 50)
IntroLogo.Position = UDim2.new(0.5, -25, 0, 25)
IntroLogo.BackgroundTransparency = 1
IntroLogo.Image = "rbxassetid://79757823848884"
IntroLogo.ImageColor3 = Color3.fromRGB(0, 255, 204)
IntroLogo.Parent = IntroFrame

local IntroTitle = Instance.new("TextLabel")
IntroTitle.Size = UDim2.new(1, 0, 0, 30)
IntroTitle.Position = UDim2.new(0, 0, 0, 85)
IntroTitle.BackgroundTransparency = 1
IntroTitle.Font = Enum.Font.GothamBold
IntroTitle.Text = "RUISHI HUB | LOADING..."
IntroTitle.TextColor3 = Color3.fromRGB(0, 255, 204)
IntroTitle.TextSize = 14
IntroTitle.Parent = IntroFrame

local BarBg = Instance.new("Frame")
BarBg.Size = UDim2.new(0, 300, 0, 8)
BarBg.Position = UDim2.new(0.5, -150, 0, 140)
BarBg.BackgroundColor3 = Color3.fromRGB(30, 30, 45)
BarBg.BorderSizePixel = 0
BarBg.Parent = IntroFrame

local BarBgCorner = Instance.new("UICorner")
BarBgCorner.CornerRadius = UDim.new(1, 0)
BarBgCorner.Parent = BarBg

local BarFill = Instance.new("Frame")
BarFill.Size = UDim2.new(0, 0, 1, 0)
BarFill.BackgroundColor3 = Color3.fromRGB(0, 255, 204)
BarFill.BorderSizePixel = 0
BarFill.Parent = BarBg

local BarFillCorner = Instance.new("UICorner")
BarFillCorner.CornerRadius = UDim.new(1, 0)
BarFillCorner.Parent = BarFill

local StatusText = Instance.new("TextLabel")
StatusText.Size = UDim2.new(1, 0, 0, 20)
StatusText.Position = UDim2.new(0, 0, 0, 165)
StatusText.BackgroundTransparency = 1
StatusText.Font = Enum.Font.GothamMedium
StatusText.Text = "Initializing modules..."
StatusText.TextColor3 = Color3.fromRGB(180, 180, 200)
StatusText.TextSize = 12
StatusText.Parent = IntroFrame

--========================================================--
-- MAIN FRAME
--========================================================--
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 560, 0, 360)
MainFrame.Position = UDim2.new(0.5, -280, 0.5, -180)
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 25)
MainFrame.BorderSizePixel = 0
MainFrame.BackgroundTransparency = 1
MainFrame.Visible = false
MainFrame.Parent = ScreenGui

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 12)
MainCorner.Parent = MainFrame

local MainStroke = Instance.new("UIStroke")
MainStroke.Color = Color3.fromRGB(0, 255, 204)
MainStroke.Thickness = 2
MainStroke.Transparency = 1
MainStroke.Parent = MainFrame

-- Top Bar Header
local TopBar = Instance.new("Frame")
TopBar.Name = "TopBar"
TopBar.Size = UDim2.new(1, 0, 0, 40)
TopBar.BackgroundColor3 = Color3.fromRGB(25, 25, 40)
TopBar.BackgroundTransparency = 1
TopBar.BorderSizePixel = 0
TopBar.Parent = MainFrame

local TitleLabel = Instance.new("TextLabel")
TitleLabel.Size = UDim2.new(1, -50, 1, 0)
TitleLabel.Position = UDim2.new(0, 15, 0, 0)
TitleLabel.BackgroundTransparency = 1
TitleLabel.Font = Enum.Font.GothamBold
TitleLabel.Text = "Ruishi Hub | Fisch v0.1"
TitleLabel.TextColor3 = Color3.fromRGB(0, 255, 204)
TitleLabel.TextTransparency = 1
TitleLabel.TextSize = 13
TitleLabel.TextXAlignment = Enum.TextXAlignment.Left
TitleLabel.Parent = TopBar

local CloseButton = Instance.new("TextButton")
CloseButton.Name = "CloseButton"
CloseButton.Size = UDim2.new(0, 28, 0, 28)
CloseButton.Position = UDim2.new(1, -34, 0, 6)
CloseButton.BackgroundColor3 = Color3.fromRGB(40, 40, 60)
CloseButton.BackgroundTransparency = 1
CloseButton.Font = Enum.Font.GothamBold
CloseButton.Text = "X"
CloseButton.TextColor3 = Color3.fromRGB(255, 80, 80)
CloseButton.TextTransparency = 1
CloseButton.TextSize = 13
CloseButton.AutoButtonColor = false
CloseButton.Parent = TopBar

local CloseCorner = Instance.new("UICorner")
CloseCorner.CornerRadius = UDim.new(0, 6)
CloseCorner.Parent = CloseButton

-- Sidebar Navigation
local Sidebar = Instance.new("Frame")
Sidebar.Name = "Sidebar"
Sidebar.Size = UDim2.new(0, 130, 1, -40)
Sidebar.Position = UDim2.new(0, 0, 0, 40)
Sidebar.BackgroundColor3 = Color3.fromRGB(20, 20, 32)
Sidebar.BackgroundTransparency = 1
Sidebar.BorderSizePixel = 0
Sidebar.Parent = MainFrame

local SideLayout = Instance.new("UIListLayout")
SideLayout.SortOrder = Enum.SortOrder.LayoutOrder
SideLayout.Padding = UDim.new(0, 4)
SideLayout.Parent = Sidebar

local SidePadding = Instance.new("UIPadding")
SidePadding.PaddingTop = UDim.new(0, 10)
SidePadding.PaddingLeft = UDim.new(0, 8)
SidePadding.PaddingRight = UDim.new(0, 8)
SidePadding.Parent = Sidebar

-- Content Container
local ContentPanel = Instance.new("Frame")
ContentPanel.Name = "ContentPanel"
ContentPanel.Size = UDim2.new(1, -140, 1, -50)
ContentPanel.Position = UDim2.new(0, 135, 0, 45)
ContentPanel.BackgroundTransparency = 1
ContentPanel.Parent = MainFrame

local Categories = {}
local TabButtons = {}

local function createTab(name)
	local scroll = Instance.new("ScrollingFrame")
	scroll.Name = name .. "Tab"
	scroll.Size = UDim2.new(1, 0, 1, 0)
	scroll.BackgroundTransparency = 1
	scroll.BorderSizePixel = 0
	scroll.ScrollBarThickness = 3
	scroll.ScrollBarImageColor3 = Color3.fromRGB(0, 255, 204)
	scroll.Visible = false
	scroll.CanvasSize = UDim2.new(0, 0, 0, 0)
	scroll.AutomaticCanvasSize = Enum.AutomaticSize.Y
	scroll.Parent = ContentPanel

	local layout = Instance.new("UIListLayout")
	layout.SortOrder = Enum.SortOrder.LayoutOrder
	layout.Padding = UDim.new(0, 8)
	layout.Parent = scroll

	local pad = Instance.new("UIPadding")
	pad.PaddingRight = UDim.new(0, 8)
	pad.Parent = scroll

	local header = Instance.new("TextLabel")
	header.Size = UDim2.new(1, 0, 0, 20)
	header.BackgroundTransparency = 1
	header.Font = Enum.Font.GothamBold
	header.Text = name:upper()
	header.TextColor3 = Color3.fromRGB(0, 255, 204)
	header.TextTransparency = 1
	header.TextSize = 12
	header.TextXAlignment = Enum.TextXAlignment.Left
	header.Parent = scroll

	Categories[name] = scroll
	return scroll
end

local function selectTab(name)
	for tabName, frame in pairs(Categories) do
		frame.Visible = (tabName == name)
	end
	for btnName, btn in pairs(TabButtons) do
		if btnName == name then
			btn.BackgroundColor3 = Color3.fromRGB(30, 30, 50)
			btn.TextColor3 = Color3.fromRGB(0, 255, 204)
		else
			btn.BackgroundColor3 = Color3.fromRGB(20, 20, 32)
			btn.TextColor3 = Color3.fromRGB(160, 160, 180)
		end
	end
end

local function createTabButton(name, order)
	local btn = Instance.new("TextButton")
	btn.Name = name .. "TabBtn"
	btn.LayoutOrder = order
	btn.Size = UDim2.new(1, 0, 0, 32)
	btn.BackgroundColor3 = Color3.fromRGB(20, 20, 32)
	btn.BackgroundTransparency = 1
	btn.BorderSizePixel = 0
	btn.Font = Enum.Font.GothamSemibold
	btn.Text = name
	btn.TextColor3 = Color3.fromRGB(160, 160, 180)
	btn.TextTransparency = 1
	btn.TextSize = 12
	btn.TextXAlignment = Enum.TextXAlignment.Left
	btn.AutoButtonColor = false
	btn.Parent = Sidebar

	local btnPad = Instance.new("UIPadding")
	btnPad.PaddingLeft = UDim.new(0, 10)
	btnPad.Parent = btn

	local btnCorner = Instance.new("UICorner")
	btnCorner.CornerRadius = UDim.new(0, 6)
	btnCorner.Parent = btn

	TabButtons[name] = btn
	btn.MouseButton1Click:Connect(function() selectTab(name) end)
	return btn
end

-- Initialize Categories in Priority Order
local AboutTab = createTab("About")
local PlayerTab = createTab("Player")
local IslandsTab = createTab("Islands TP")
local NpcsTab = createTab("NPCs TP")
local AutoBuyTab = createTab("Auto Buy")
local SettingsTab = createTab("Settings")

createTabButton("About", 1)
createTabButton("Player", 2)
createTabButton("Islands TP", 3)
createTabButton("NPCs TP", 4)
createTabButton("Auto Buy", 5)
createTabButton("Settings", 6)

--========================================================--
-- COMPONENT HELPERS
--========================================================--
local function createToggle(parent, name, callback)
	local container = Instance.new("Frame")
	container.Size = UDim2.new(1, 0, 0, 38)
	container.BackgroundColor3 = Color3.fromRGB(22, 22, 35)
	container.BackgroundTransparency = 1
	container.BorderSizePixel = 0
	container.Parent = parent

	local corner = Instance.new("UICorner")
	corner.CornerRadius = UDim.new(0, 6)
	corner.Parent = container

	local label = Instance.new("TextLabel")
	label.Size = UDim2.new(1, -50, 1, 0)
	label.Position = UDim2.new(0, 10, 0, 0)
	label.BackgroundTransparency = 1
	label.Font = Enum.Font.GothamMedium
	label.Text = name
	label.TextColor3 = Color3.fromRGB(210, 210, 230)
	label.TextTransparency = 1
	label.TextSize = 12
	label.TextXAlignment = Enum.TextXAlignment.Left
	label.Parent = container

	local toggleBtn = Instance.new("TextButton")
	toggleBtn.Size = UDim2.new(0, 36, 0, 18)
	toggleBtn.Position = UDim2.new(1, -44, 0.5, -9)
	toggleBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 60)
	toggleBtn.BackgroundTransparency = 1
	toggleBtn.Text = ""
	toggleBtn.AutoButtonColor = false
	toggleBtn.Parent = container

	local btnCorner = Instance.new("UICorner")
	btnCorner.CornerRadius = UDim.new(1, 0)
	btnCorner.Parent = toggleBtn

	local circle = Instance.new("Frame")
	circle.Size = UDim2.new(0, 14, 0, 14)
	circle.Position = UDim2.new(0, 2, 0.5, -7)
	circle.BackgroundColor3 = Color3.fromRGB(180, 180, 200)
	circle.BackgroundTransparency = 1
	circle.BorderSizePixel = 0
	circle.Parent = toggleBtn

	local circleCorner = Instance.new("UICorner")
	circleCorner.CornerRadius = UDim.new(1, 0)
	circleCorner.Parent = circle

	local toggled = false
	local function updateState(state)
		toggled = state
		if toggled then
			circle:TweenPosition(UDim2.new(1, -16, 0.5, -7), Enum.EasingDirection.Out, Enum.EasingStyle.Quad, 0.15, true)
			toggleBtn.BackgroundColor3 = Color3.fromRGB(0, 255, 204)
			circle.BackgroundColor3 = Color3.fromRGB(15, 15, 25)
		else
			circle:TweenPosition(UDim2.new(0, 2, 0.5, -7), Enum.EasingDirection.Out, Enum.EasingStyle.Quad, 0.15, true)
			toggleBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 60)
			circle.BackgroundColor3 = Color3.fromRGB(180, 180, 200)
		end
		callback(toggled)
	end

	toggleBtn.MouseButton1Click:Connect(function()
		updateState(not toggled)
	end)

	return { Set = updateState }
end

local function createSlider(parent, name, min, default, max, callback)
	local container = Instance.new("Frame")
	container.Size = UDim2.new(1, 0, 0, 46)
	container.BackgroundColor3 = Color3.fromRGB(22, 22, 35)
	container.BackgroundTransparency = 1
	container.BorderSizePixel = 0
	container.Parent = parent

	local corner = Instance.new("UICorner")
	corner.CornerRadius = UDim.new(0, 6)
	corner.Parent = container

	local label = Instance.new("TextLabel")
	label.Size = UDim2.new(1, -10, 0, 20)
	label.Position = UDim2.new(0, 10, 0, 4)
	label.BackgroundTransparency = 1
	label.Font = Enum.Font.GothamMedium
	label.Text = name .. ": " .. default
	label.TextColor3 = Color3.fromRGB(210, 210, 230)
	label.TextTransparency = 1
	label.TextSize = 12
	label.TextXAlignment = Enum.TextXAlignment.Left
	label.Parent = container

	local sliderBg = Instance.new("Frame")
	sliderBg.Name = "SliderBg"
	sliderBg.Size = UDim2.new(1, -20, 0, 6)
	sliderBg.Position = UDim2.new(0, 10, 0, 28)
	sliderBg.BackgroundColor3 = Color3.fromRGB(40, 40, 60)
	sliderBg.BackgroundTransparency = 1
	sliderBg.BorderSizePixel = 0
	sliderBg.Parent = container

	local bgCorner = Instance.new("UICorner")
	bgCorner.CornerRadius = UDim.new(1, 0)
	bgCorner.Parent = sliderBg

	local sliderFill = Instance.new("Frame")
	sliderFill.Name = "SliderFill"
	sliderFill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
	sliderFill.BackgroundColor3 = Color3.fromRGB(0, 255, 204)
	sliderFill.BackgroundTransparency = 1
	sliderFill.BorderSizePixel = 0
	sliderFill.Parent = sliderBg

	local fillCorner = Instance.new("UICorner")
	fillCorner.CornerRadius = UDim.new(1, 0)
	fillCorner.Parent = sliderFill

	local dragging = false
	sliderBg.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
			dragging = true
		end
	end)
	UserInputService.InputEnded:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
			dragging = false
		end
	end)
	UserInputService.InputChanged:Connect(function(input)
		if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
			local pos = math.clamp((input.Position.X - sliderBg.AbsolutePosition.X) / sliderBg.AbsoluteSize.X, 0, 1)
			sliderFill.Size = UDim2.new(pos, 0, 1, 0)
			local val = math.floor(min + (max - min) * pos)
			label.Text = name .. ": " .. val
			callback(val)
		end
	end)
end

local function createDropdown(parent, placeholder, options, callback)
	local container = Instance.new("Frame")
	container.Size = UDim2.new(1, 0, 0, 36)
	container.BackgroundColor3 = Color3.fromRGB(22, 22, 35)
	container.BackgroundTransparency = 1
	container.BorderSizePixel = 0
	container.ZIndex = 5
	container.Parent = parent

	local corner = Instance.new("UICorner")
	corner.CornerRadius = UDim.new(0, 6)
	corner.Parent = container

	local dropBtn = Instance.new("TextButton")
	dropBtn.Size = UDim2.new(1, 0, 1, 0)
	dropBtn.BackgroundTransparency = 1
	dropBtn.Font = Enum.Font.GothamMedium
	dropBtn.Text = "  " .. placeholder
	dropBtn.TextColor3 = Color3.fromRGB(0, 255, 204)
	dropBtn.TextTransparency = 1
	dropBtn.TextSize = 12
	dropBtn.TextXAlignment = Enum.TextXAlignment.Left
	dropBtn.ZIndex = 6
	dropBtn.Parent = container

	local dropList = Instance.new("ScrollingFrame")
	dropList.Size = UDim2.new(1, 0, 0, 120)
	dropList.Position = UDim2.new(0, 0, 1, 4)
	dropList.BackgroundColor3 = Color3.fromRGB(28, 28, 42)
	dropList.BorderSizePixel = 0
	dropList.ScrollBarThickness = 3
	dropList.ScrollBarImageColor3 = Color3.fromRGB(0, 255, 204)
	dropList.Visible = false
	dropList.ZIndex = 10
	dropList.CanvasSize = UDim2.new(0, 0, 0, 0)
	dropList.AutomaticCanvasSize = Enum.AutomaticSize.Y
	dropList.Parent = container

	local listLayout = Instance.new("UIListLayout")
	listLayout.SortOrder = Enum.SortOrder.LayoutOrder
	listLayout.Parent = dropList

	for _, optName in ipairs(options) do
		local opt = Instance.new("TextButton")
		opt.Size = UDim2.new(1, 0, 0, 26)
		opt.BackgroundTransparency = 1
		opt.Font = Enum.Font.GothamMedium
		opt.Text = "  " .. optName
		opt.TextColor3 = Color3.fromRGB(200, 200, 220)
		opt.TextSize = 11
		opt.TextXAlignment = Enum.TextXAlignment.Left
		opt.ZIndex = 11
		opt.Parent = dropList

		opt.MouseButton1Click:Connect(function()
			dropBtn.Text = "  " .. optName
			dropList.Visible = false
			callback(optName)
		end)
	end

	dropBtn.MouseButton1Click:Connect(function()
		dropList.Visible = not dropList.Visible
	end)
end

--========================================================--
-- CATEGORY CONTENT POPULATION
--========================================================--

-- 1. ABOUT TAB
local AboutText = Instance.new("TextLabel")
AboutText.Size = UDim2.new(1, 0, 0, 140)
AboutText.BackgroundTransparency = 1
AboutText.Font = Enum.Font.GothamMedium
AboutText.Text = "DISCLAIMER:\nUse this script at your own risk. The developers are not responsible for any account bans, strikes, or penalties incurred. It is strongly recommended to test all features on an alternate or smurf account before using it on your primary account."
AboutText.TextColor3 = Color3.fromRGB(220, 180, 100)
AboutText.TextTransparency = 1
AboutText.TextSize = 12
AboutText.TextWrapped = true
AboutText.TextYAlignment = Enum.TextYAlignment.Top
AboutText.Parent = AboutTab

-- 2. PLAYER TAB (0-500 WalkSpeed/JumpPower, Fly, Walk Water)
local currentSpeed = 16
local currentJump = 50
local flyEnabled = false
local flySpeed = 50
local walkWaterEnabled = false

createSlider(PlayerTab, "WalkSpeed", 0, 16, 500, function(v)
	currentSpeed = v
	if player.Character and player.Character:FindFirstChild("Humanoid") then
		player.Character.Humanoid.WalkSpeed = currentSpeed
	end
end)

createSlider(PlayerTab, "JumpPower", 0, 50, 500, function(v)
	currentJump = v
	if player.Character and player.Character:FindFirstChild("Humanoid") then
		player.Character.Humanoid.UseJumpPower = true
		player.Character.Humanoid.JumpPower = currentJump
	end
end)

createToggle(PlayerTab, "Fly Mode", function(state) flyEnabled = state end)
createSlider(PlayerTab, "Fly Speed", 0, 50, 100, function(v) flySpeed = v end)
createToggle(PlayerTab, "Walk On Water", function(state) walkWaterEnabled = state end)
createToggle(PlayerTab, "Noclip", function(state) _G.Noclip = state end)
createToggle(PlayerTab, "Infinite Jump", function(state) _G.InfJump = state end)

-- Fly System Implementation
task.spawn(function()
	local bodyVel, bodyGyro
	RunService.RenderStepped:Connect(function()
		local char = player.Character
		if flyEnabled and char and char:FindFirstChild("HumanoidRootPart") then
			local hrp = char.HumanoidRootPart
			if not bodyVel or bodyVel.Parent ~= hrp then
				bodyVel = Instance.new("BodyVelocity")
				bodyVel.MaxForce = Vector3.new(1e9, 1e9, 1e9)
				bodyVel.Parent = hrp
			end
			if not bodyGyro or bodyGyro.Parent ~= hrp then
				bodyGyro = Instance.new("BodyGyro")
				bodyGyro.MaxTorque = Vector3.new(1e9, 1e9, 1e9)
				bodyGyro.P = 9000
				bodyGyro.Parent = hrp
			end

			local cam = workspace.CurrentCamera
			local moveDir = Vector3.zero
			if UserInputService:IsKeyDown(Enum.KeyCode.W) then moveDir = moveDir + cam.CFrame.LookVector end
			if UserInputService:IsKeyDown(Enum.KeyCode.S) then moveDir = moveDir - cam.CFrame.LookVector end
			if UserInputService:IsKeyDown(Enum.KeyCode.A) then moveDir = moveDir - cam.CFrame.RightVector end
			if UserInputService:IsKeyDown(Enum.KeyCode.D) then moveDir = moveDir + cam.CFrame.RightVector end
			if UserInputService:IsKeyDown(Enum.KeyCode.Space) then moveDir = moveDir + Vector3.new(0, 1, 0) end
			if UserInputService:IsKeyDown(Enum.KeyCode.LeftShift) then moveDir = moveDir - Vector3.new(0, 1, 0) end

			bodyVel.Velocity = moveDir * flySpeed
			bodyGyro.CFrame = cam.CFrame
		else
			if bodyVel then bodyVel:Destroy() bodyVel = nil end
			if bodyGyro then bodyGyro:Destroy() bodyGyro = nil end
		end
	end)
end)

-- Walk On Water Platform Logic
task.spawn(function()
	local waterPlatform = Instance.new("Part")
	waterPlatform.Size = Vector3.new(10, 1, 10)
	waterPlatform.Anchored = true
	waterPlatform.Transparency = 1
	waterPlatform.CanCollide = false
	waterPlatform.Parent = workspace

	RunService.RenderStepped:Connect(function()
		local char = player.Character
		if walkWaterEnabled and char and char:FindFirstChild("HumanoidRootPart") then
			local hrp = char.HumanoidRootPart
			local ray = Ray.new(hrp.Position, Vector3.new(0, -15, 0))
			local hit, pos, _, material = workspace:FindPartOnRay(ray, char)
			if material == Enum.Material.Water or (hit and hit.Name == "Water") then
				waterPlatform.CFrame = CFrame.new(hrp.Position.X, pos.Y - 0.5, hrp.Position.Z)
				waterPlatform.CanCollide = true
			else
				waterPlatform.CanCollide = false
			end
		else
			waterPlatform.CanCollide = false
		end
	end)
end)

-- 3. ISLANDS TELEPORT TAB
local islandList = {}
for name in pairs(Islands) do table.insert(islandList, name) end
table.sort(islandList)

local selectedIslandPos = nil
createDropdown(IslandsTab, "Select Island Spot...", islandList, function(selected)
	selectedIslandPos = Islands[selected]
end)

local IslandTpBtn = Instance.new("TextButton")
IslandTpBtn.Size = UDim2.new(1, 0, 0, 36)
IslandTpBtn.BackgroundColor3 = Color3.fromRGB(0, 255, 204)
IslandTpBtn.BackgroundTransparency = 1
IslandTpBtn.Font = Enum.Font.GothamBold
IslandTpBtn.Text = "TELEPORT TO ISLAND"
IslandTpBtn.TextColor3 = Color3.fromRGB(15, 15, 25)
IslandTpBtn.TextTransparency = 1
IslandTpBtn.TextSize = 12
IslandTpBtn.AutoButtonColor = false
IslandTpBtn.Parent = IslandsTab

local IslandTpCorner = Instance.new("UICorner")
IslandTpCorner.CornerRadius = UDim.new(0, 6)
IslandTpCorner.Parent = IslandTpBtn

IslandTpBtn.MouseButton1Click:Connect(function()
	if selectedIslandPos and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
		player.Character.HumanoidRootPart.CFrame = CFrame.new(selectedIslandPos + Vector3.new(0, 5, 0))
	end
end)

-- 4. NPCS TELEPORT TAB
local npcList = {}
for name in pairs(NPCs) do table.insert(npcList, name) end
table.sort(npcList)

local selectedNpcPos = nil
createDropdown(NpcsTab, "Select NPC Spot...", npcList, function(selected)
	selectedNpcPos = NPCs[selected]
end)

local NpcTpBtn = Instance.new("TextButton")
NpcTpBtn.Size = UDim2.new(1, 0, 0, 36)
NpcTpBtn.BackgroundColor3 = Color3.fromRGB(0, 255, 204)
NpcTpBtn.BackgroundTransparency = 1
NpcTpBtn.Font = Enum.Font.GothamBold
NpcTpBtn.Text = "TELEPORT TO NPC"
NpcTpBtn.TextColor3 = Color3.fromRGB(15, 15, 25)
NpcTpBtn.TextTransparency = 1
NpcTpBtn.TextSize = 12
NpcTpBtn.AutoButtonColor = false
NpcTpBtn.Parent = NpcsTab

local NpcTpCorner = Instance.new("UICorner")
NpcTpCorner.CornerRadius = UDim.new(0, 6)
NpcTpCorner.Parent = NpcTpBtn

NpcTpBtn.MouseButton1Click:Connect(function()
	if selectedNpcPos and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
		player.Character.HumanoidRootPart.CFrame = CFrame.new(selectedNpcPos + Vector3.new(0, 5, 0))
	end
end)

-- 5. AUTO BUY TAB (ITEM SELECTION + TYPED QUANTITY + BUY BUTTON)
local selectedBaitItem = "Bait Crate"
local typedAmount = 1

createDropdown(AutoBuyTab, "Select Item...", BaitItems, function(selected)
	selectedBaitItem = selected
end)

local QtyBoxContainer = Instance.new("Frame")
QtyBoxContainer.Size = UDim2.new(1, 0, 0, 36)
QtyBoxContainer.BackgroundColor3 = Color3.fromRGB(22, 22, 35)
QtyBoxContainer.BackgroundTransparency = 1
QtyBoxContainer.BorderSizePixel = 0
QtyBoxContainer.Parent = AutoBuyTab

local QtyBoxCorner = Instance.new("UICorner")
QtyBoxCorner.CornerRadius = UDim.new(0, 6)
QtyBoxCorner.Parent = QtyBoxContainer

local QtyTextBox = Instance.new("TextBox")
QtyTextBox.Size = UDim2.new(1, 0, 1, 0)
QtyTextBox.BackgroundTransparency = 1
QtyTextBox.Font = Enum.Font.GothamMedium
QtyTextBox.PlaceholderText = "Enter Quantity (e.g. 5)"
QtyTextBox.Text = "1"
QtyTextBox.TextColor3 = Color3.fromRGB(0, 255, 204)
QtyTextBox.TextTransparency = 1
QtyTextBox.TextSize = 12
QtyTextBox.TextXAlignment = Enum.TextXAlignment.Left
QtyTextBox.Parent = QtyBoxContainer

local QtyPad = Instance.new("UIPadding")
QtyPad.PaddingLeft = UDim.new(0, 10)
QtyPad.Parent = QtyTextBox

QtyTextBox.FocusLost:Connect(function()
	local num = tonumber(QtyTextBox.Text)
	if num and num > 0 then
		typedAmount = math.floor(num)
	else
		QtyTextBox.Text = "1"
		typedAmount = 1
	end
end)

local BuyItemBtn = Instance.new("TextButton")
BuyItemBtn.Size = UDim2.new(1, 0, 0, 36)
BuyItemBtn.BackgroundColor3 = Color3.fromRGB(0, 255, 204)
BuyItemBtn.BackgroundTransparency = 1
BuyItemBtn.Font = Enum.Font.GothamBold
BuyItemBtn.Text = "BUY BAIT OR CRATE"
BuyItemBtn.TextColor3 = Color3.fromRGB(15, 15, 25)
BuyItemBtn.TextTransparency = 1
BuyItemBtn.TextSize = 12
BuyItemBtn.AutoButtonColor = false
BuyItemBtn.Parent = AutoBuyTab

local BuyItemCorner = Instance.new("UICorner")
BuyItemCorner.CornerRadius = UDim.new(0, 6)
BuyItemCorner.Parent = BuyItemBtn

BuyItemBtn.MouseButton1Click:Connect(function()
	task.spawn(function()
		BuyItemBtn.Text = "PURCHASING..."
		for i = 1, typedAmount do
			pcall(function()
				if ReplicatedStorage:FindFirstChild("events") and ReplicatedStorage.events:FindFirstChild("purchase") then
					ReplicatedStorage.events.purchase:FireServer(selectedBaitItem)
				elseif ReplicatedStorage:FindFirstChild("packages") and ReplicatedStorage.packages:FindFirstChild("Net") then
					ReplicatedStorage.packages.Net["RE/PurchaseBaitCrate"]:FireServer(selectedBaitItem)
				end
			end)
			task.wait(0.05)
		end
		BuyItemBtn.Text = "PURCHASE COMPLETE"
		task.wait(1)
		BuyItemBtn.Text = "BUY BAIT OR CRATE"
	end)
end)

-- 6. SETTINGS TAB (REJOIN & SERVER HOP)
local RejoinBtn = Instance.new("TextButton")
RejoinBtn.Size = UDim2.new(1, 0, 0, 36)
RejoinBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 48)
RejoinBtn.BackgroundTransparency = 1
RejoinBtn.Font = Enum.Font.GothamBold
RejoinBtn.Text = "REJOIN SERVER"
RejoinBtn.TextColor3 = Color3.fromRGB(0, 255, 204)
RejoinBtn.TextTransparency = 1
RejoinBtn.TextSize = 12
RejoinBtn.AutoButtonColor = false
RejoinBtn.Parent = SettingsTab

local RejoinCorner = Instance.new("UICorner")
RejoinCorner.CornerRadius = UDim.new(0, 6)
RejoinCorner.Parent = RejoinBtn

RejoinBtn.MouseButton1Click:Connect(function()
	TeleportService:TeleportToPlaceInstance(game.PlaceId, game.JobId, player)
end)

local ServerHopBtn = Instance.new("TextButton")
ServerHopBtn.Size = UDim2.new(1, 0, 0, 36)
ServerHopBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 48)
ServerHopBtn.BackgroundTransparency = 1
ServerHopBtn.Font = Enum.Font.GothamBold
ServerHopBtn.Text = "SERVER HOP"
ServerHopBtn.TextColor3 = Color3.fromRGB(0, 255, 204)
ServerHopBtn.TextTransparency = 1
ServerHopBtn.TextSize = 12
ServerHopBtn.AutoButtonColor = false
ServerHopBtn.Parent = SettingsTab

local ServerHopCorner = Instance.new("UICorner")
ServerHopCorner.CornerRadius = UDim.new(0, 6)
ServerHopCorner.Parent = ServerHopBtn

ServerHopBtn.MouseButton1Click:Connect(function()
	pcall(function()
		local HttpService = game:GetService("HttpService")
		local servers = HttpService:JSONDecode(game:HttpGet("https://games.roblox.com/v1/games/" .. game.PlaceId .. "/servers/Public?sortOrder=Asc&limit=100"))
		for _, s in ipairs(servers.data) do
			if s.id ~= game.JobId and s.playing < s.maxPlayers then
				TeleportService:TeleportToPlaceInstance(game.PlaceId, s.id, player)
				break
			end
		end
	end)
end)

selectTab("About")

--========================================================--
-- MOBILE TOGGLE BUTTON
--========================================================--
local MobileButton = Instance.new("ImageButton")
MobileButton.Name = "MobileToggle"
MobileButton.Size = UDim2.new(0, 48, 0, 48)
MobileButton.Position = UDim2.new(0, 20, 0.5, -24)
MobileButton.BackgroundColor3 = Color3.fromRGB(15, 15, 25)
MobileButton.BackgroundTransparency = 1
MobileButton.Image = "rbxassetid://79757823848884"
MobileButton.ImageColor3 = Color3.fromRGB(0, 255, 204)
MobileButton.ImageTransparency = 1
MobileButton.Visible = false
MobileButton.Parent = ScreenGui

local MobCorner = Instance.new("UICorner")
MobCorner.CornerRadius = UDim.new(1, 0)
MobCorner.Parent = MobileButton

local MobStroke = Instance.new("UIStroke")
MobStroke.Color = Color3.fromRGB(0, 255, 204)
MobStroke.Thickness = 2
MobStroke.Transparency = 1
MobStroke.Parent = MobileButton

--========================================================--
-- INTRO ANIMATION & FADE IN EXECUTION
--========================================================--
task.spawn(function()
	task.wait(0.3)
	StatusText.Text = "Loading bypass & movement features..."
	BarFill:TweenSize(UDim2.new(0.6, 0, 1, 0), Enum.EasingDirection.Out, Enum.EasingStyle.Quad, 0.8, true)
	task.wait(0.9)
	StatusText.Text = "Starting UI environment..."
	BarFill:TweenSize(UDim2.new(1, 0, 1, 0), Enum.EasingDirection.Out, Enum.EasingStyle.Quad, 0.6, true)
	task.wait(0.7)
	StatusText.Text = "Welcome!"
	task.wait(0.4)

	local introTweenInfo = TweenInfo.new(0.6, Enum.EasingStyle.Quad, Enum.EasingDirection.Out)
	TweenService:Create(IntroFrame, introTweenInfo, {BackgroundTransparency = 1}):Play()
	TweenService:Create(IntroStroke, introTweenInfo, {Transparency = 1}):Play()
	TweenService:Create(IntroLogo, introTweenInfo, {ImageTransparency = 1}):Play()
	TweenService:Create(IntroTitle, introTweenInfo, {TextTransparency = 1}):Play()
	TweenService:Create(StatusText, introTweenInfo, {TextTransparency = 1}):Play()
	TweenService:Create(BarBg, introTweenInfo, {BackgroundTransparency = 1}):Play()
	TweenService:Create(BarFill, introTweenInfo, {BackgroundTransparency = 1}):Play()

	task.wait(0.6)
	IntroGui:Destroy()

	MainFrame.Visible = true
	MobileButton.Visible = true

	local fadeInInfo = TweenInfo.new(0.5, Enum.EasingStyle.Quad, Enum.EasingDirection.Out)

	TweenService:Create(MainFrame, fadeInInfo, {BackgroundTransparency = 0}):Play()
	TweenService:Create(MainStroke, fadeInInfo, {Transparency = 0}):Play()
	TweenService:Create(TopBar, fadeInInfo, {BackgroundTransparency = 0}):Play()
	TweenService:Create(TitleLabel, fadeInInfo, {TextTransparency = 0}):Play()
	TweenService:Create(CloseButton, fadeInInfo, {BackgroundTransparency = 0, TextTransparency = 0}):Play()
	TweenService:Create(Sidebar, fadeInInfo, {BackgroundTransparency = 0}):Play()

	TweenService:Create(MobileButton, fadeInInfo, {BackgroundTransparency = 0, ImageTransparency = 0}):Play()
	TweenService:Create(MobStroke, fadeInInfo, {Transparency = 0}):Play()

	for _, child in ipairs(MainFrame:GetDescendants()) do
		if child:IsA("TextLabel") or child:IsA("TextBox") then
			TweenService:Create(child, fadeInInfo, {TextTransparency = 0}):Play()
		elseif child:IsA("Frame") then
			if child.Name == "SliderBg" then
				TweenService:Create(child, fadeInInfo, {BackgroundTransparency = 0}):Play()
			elseif child.Name ~= "SliderFill" and child.Name ~= "ContentPanel" then
				TweenService:Create(child, fadeInInfo, {BackgroundTransparency = 0}):Play()
			end
		elseif child:IsA("TextButton") then
			TweenService:Create(child, fadeInInfo, {BackgroundTransparency = 0, TextTransparency = 0}):Play()
		end
	end
end)

--========================================================--
-- CLOSE & TOGGLE SYSTEMS
--========================================================--
local isOpen = true

local function toggleUI()
	isOpen = not isOpen
	if isOpen then
		MainFrame.Visible = true
		local fadeInInfo = TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out)
		TweenService:Create(MainFrame, fadeInInfo, {BackgroundTransparency = 0}):Play()
		TweenService:Create(MainStroke, fadeInInfo, {Transparency = 0}):Play()
		TweenService:Create(TopBar, fadeInInfo, {BackgroundTransparency = 0}):Play()
		TweenService:Create(TitleLabel, fadeInInfo, {TextTransparency = 0}):Play()
		TweenService:Create(CloseButton, fadeInInfo, {BackgroundTransparency = 0, TextTransparency = 0}):Play()
		TweenService:Create(Sidebar, fadeInInfo, {BackgroundTransparency = 0}):Play()
		for _, child in ipairs(MainFrame:GetDescendants()) do
			if child:IsA("TextLabel") or child:IsA("TextBox") then
				TweenService:Create(child, fadeInInfo, {TextTransparency = 0}):Play()
			elseif child:IsA("Frame") then
				if child.Name == "SliderBg" then
					TweenService:Create(child, fadeInInfo, {BackgroundTransparency = 0}):Play()
				elseif child.Name ~= "SliderFill" and child.Name ~= "ContentPanel" then
					TweenService:Create(child, fadeInInfo, {BackgroundTransparency = 0}):Play()
				end
			elseif child:IsA("TextButton") then
				TweenService:Create(child, fadeInInfo, {BackgroundTransparency = 0, TextTransparency = 0}):Play()
			end
		end
	else
		local fadeOutInfo = TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out)
		TweenService:Create(MainFrame, fadeOutInfo, {BackgroundTransparency = 1}):Play()
		TweenService:Create(MainStroke, fadeOutInfo, {Transparency = 1}):Play()
		TweenService:Create(TopBar, fadeOutInfo, {BackgroundTransparency = 1}):Play()
		TweenService:Create(TitleLabel, fadeOutInfo, {TextTransparency = 1}):Play()
		TweenService:Create(CloseButton, fadeOutInfo, {BackgroundTransparency = 1, TextTransparency = 1}):Play()
		TweenService:Create(Sidebar, fadeOutInfo, {BackgroundTransparency = 1}):Play()
		for _, child in ipairs(MainFrame:GetDescendants()) do
			if child:IsA("TextLabel") or child:IsA("TextBox") then
				TweenService:Create(child, fadeOutInfo, {TextTransparency = 1}):Play()
			elseif child:IsA("Frame") then
				TweenService:Create(child, fadeOutInfo, {BackgroundTransparency = 1}):Play()
			elseif child:IsA("TextButton") then
				TweenService:Create(child, fadeOutInfo, {BackgroundTransparency = 1, TextTransparency = 1}):Play()
			end
		end
		task.wait(0.3)
		MainFrame.Visible = false
	end
end

CloseButton.MouseButton1Click:Connect(toggleUI)
MobileButton.MouseButton1Click:Connect(toggleUI)

--========================================================--
-- RUNSERVICE & DRAGGABLE LOGIC
--========================================================--
UserInputService.JumpRequest:Connect(function()
	if _G.InfJump and player.Character and player.Character:FindFirstChild("Humanoid") then
		player.Character.Humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
	end
end)

RunService.RenderStepped:Connect(function()
	local char = player.Character
	if not char then return end
	local hum = char:FindFirstChild("Humanoid")
	if hum then
		if hum.WalkSpeed ~= currentSpeed then hum.WalkSpeed = currentSpeed end
		if hum.JumpPower ~= currentJump then
			hum.UseJumpPower = true
			hum.JumpPower = currentJump
		end
	end
	if _G.Noclip then
		for _, part in ipairs(char:GetDescendants()) do
			if part:IsA("BasePart") and part.CanCollide then
				part.CanCollide = false
			end
		end
	end
end)

-- Main Window Dragging
local dragging, dragStart, startPos
TopBar.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
		dragging = true
		dragStart = input.Position
		startPos = MainFrame.Position
	end
end)

UserInputService.InputChanged:Connect(function(input)
	if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
		local delta = input.Position - dragStart
		MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
	end
end)

UserInputService.InputEnded:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
		dragging = false
	end
end)

-- Mobile Button Dragging
local mDragging, mDragStart, mStartPos
MobileButton.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
		mDragging = true
		mDragStart = input.Position
		mStartPos = MobileButton.Position
	end
end)

UserInputService.InputChanged:Connect(function(input)
	if mDragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
		local delta = input.Position - mDragStart
		MobileButton.Position = UDim2.new(mStartPos.X.Scale, mStartPos.X.Offset + delta.X, mStartPos.Y.Scale, mStartPos.Y.Offset + delta.Y)
	end
end)

UserInputService.InputEnded:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
		mDragging = false
	end
end)
